'use strict';

/**
 * @ngdoc function
 * @name canonApp.controller:NghideCtrl
 * @description
 * # NghideCtrl
 * Controller of the canonApp
 */
angular.module('canonApp')
  .controller('NgHideCtrl', function ($scope) {
    $scope.awesomeThings = [
      'HTML5 Boilerplate',
      'AngularJS',
      'Karma'
    ];
  });
