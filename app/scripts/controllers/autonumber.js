'use strict';

/**
 * @ngdoc function
 * @name canonApp.controller:AutonumberCtrl
 * @description
 * # AutonumberCtrl
 * Controller of the canonApp
 */
angular.module('canonApp')
  .controller('AutonumberCtrl', function ($scope) {
    $scope.awesomeThings = [
      'HTML5 Boilerplate',
      'AngularJS',
      'Karma'
    ];
  });
